* references
    * https://velir.atlassian.net/wiki/spaces/Sitecore/pages/5268144155/Marketplace+App+Ideas
	* https://velir.atlassian.net/wiki/spaces/Sitecore/pages/5261262884/Sitecore+Marketplace
* tooling
    * word doc processing (extract and mine)
    * site specific instructions for paths would help find things like authors and lookups
    * check if page exists before creating it
    * how to find and display an image (thumbnail and modal or new window)
    * analyze an image from the media library and set the alt text (or folder)
    * when images are generated, they show up and then more binary text continues to be added until something else finishes
    * users can choose, view or improve images
    * evaluating content, looking for empty fields, setting metadata or alt tags
    * allow for tagging which requires looking up templates, field sources and connecting where that is (relative, sxa variable)    * not all of the chat text is displayed (thinking for example) and it often isn't a resilient conversation. an error will stop the response.
    * automate scaffolding pages and structure possibly from generated content
    * analyze page content and set the metadata fields
    * any type of update function should be approved first by the user. 
    * want to manage the instructions for each profile more easily and partially in content
    * the profile selection seems off. I said 'hello' and it set it to 'component populator' more refinement and testing needed
    * make models easy to change out
    * convert content to audio (blogcast)
    * can we make it good at new requests from users. how to handle can you do this for document processing?
    * list out features
* UI
    * polish up the UI to be less web 2.0 
    * audio interface
    * need a cancel button to stop long running conversation
    * needs design for lists so they are more prominent (bullets or <li> etc) and headings. code blocks should be the width of the text not full available width
    * when i open the conversation manager and delete one item, it closes. after the first it stays open. It should just stay open until i close it.
* content 
    * is the content good enough on clarity or should we use velir.com 
    * create content in sitecore for site-specific brand voice
    * put the task prompts into content
    * instructions for image gen to make very high quality images
    * bulk update media alt text and description
* testing
    * content research
    * building a page
    * create an image then update it 
    * test multiple users and validate they have different sessions
    * test in pages 
    * features to test
        * profiles for each type of task
        * conversations are stored and available to restart, switch between or be deleted
        * if you leave a conversation for login, the conversation should persist
        * mcp connection to search and marketer/agent api 
        * image and text generation available. images should be formatted width to allow four across and should be clickable to view full size
* analytics 
    * can we draw in any data to help decision making
* deploy
    * how/where to deploy on vercel
    * get free supabase account to deploy to vercel
* app
     `images.domains` is deprecated in favor of `images.remotePatterns`.
     * structure the code more like: controllers (route handlers), services (business logic), apis (defines api methods and uses clients), clients (external connection), resources (data)
    * refactor code down to more manageable structure
    * remove unused/duplicate env variables
    